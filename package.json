{
  "name": "shoba-ai-backend",
  "version": "1.2.0",
  "private": true,
  "description": "Secure API layer and discovery engine for the SHOBA Connect WordPress business directory.",
  "engines": { "node": ">=20" },
  "scripts": { "start": "vercel dev", "test": "node tests/smoke.js" }
}
